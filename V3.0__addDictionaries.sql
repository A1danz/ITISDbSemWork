create table
  DeviceType (id bigint primary key, name varchar(40));

create table
  ClientType (id bigint primary key, name varchar(40));

create table
  EmojiCategory (id bigint primary key, name varchar(40));

create table
  FileType (id bigint primary key, name varchar(40));

create table
  Quality (id bigint primary key, quality varchar(40));

create table
  FpsCount (id bigint primary key, fps varchar(4));

create table
  ActivityGenre (id bigint primary key, name varchar(40));

create table
  RegionType (id bigint primary key, name varchar(40));